name: Counter

on:
  workflow_dispatch:
  schedule:
    - cron: '*/5 * * * *'

jobs:
  greetings:
    runs-on: ubuntu-latest
    steps:
      - name: Print greetings with current time
        run: |
          now=$(date)
          echo "Hello! The time is $now"
